# Workshop runtime outputs (regenerated on each run)
outputs/
inputs/

# Downloaded weights and parameters (pulled fresh by setup.sh — ~5GB)
*.pt
params/
RFdiffusion/
models/

# PDB structures downloaded at runtime
*.pdb
!examples/*.pdb

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# Conda
.conda/

# OS
.DS_Store
Thumbs.db
._*

# Editors
.vscode/
.idea/
*.swp
*~

# Secrets — DO NOT commit
*.pem
*.key
.env
.aws/

# --- Workshop additions (per-instance generated, regenerated by setup.sh) ---
# Per-instance launcher generated by setup.sh (paths baked in)
start_app.sh
streamlit_ui/start_app.sh

# Setup logs
setup-*.log

# Workshop run artifacts (per-team, per-run; regenerated by app.py)
streamlit_ui/inputs/
streamlit_ui/outputs/

# Large regenerable artifacts (live under ~, but defend the repo tree)
msa_cache/
params/
RFdiffusion/

# Session notes (not source)
handoff/
